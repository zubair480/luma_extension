# Optional: enable LLM answers for custom registration questions
OPENAI_API_KEY=

# Browser settings — connect to your existing Chrome (recommended)
USE_EXISTING_CHROME=true
CHROME_CDP_URL=http://127.0.0.1:9222

# Fallback: separate Playwright browser profile
HEADLESS=false
BROWSER_PROFILE_DIR=./browser_profile
